# ================================
# MONGODB CONFIGURATION
# ================================
# MongoDB connection URL
# Local: mongodb://localhost:27017
# With Auth: mongodb://username:password@localhost:27017
# MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net
MONGODB_URL=mongodb+srv://.................@cluster0.2kqc6g9.mongodb.net/?appName=Cluster0

# MongoDB database name
MONGODB_DB_NAME=project_management_system

# ================================
# JWT AUTHENTICATION
# ================================
# Secret key for JWT token signing (IMPORTANT: Change this to a random string)
# Generate using: openssl rand -hex 32
SECRET_KEY=your-secret-key-change-this-in-production

# JWT algorithm (keep as HS256 for HMAC)
ALGORITHM=HS256

# JWT token expiration time in minutes
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ================================
# CORS SETTINGS
# ================================
# Allowed origins for CORS (comma-separated for multiple origins)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ================================
# APPLICATION SETTINGS
# ================================
# Application environment (development, staging, production)
ENVIRONMENT=development

# Debug mode
DEBUG=true

# ================================
# EMAIL/SMTP CONFIGURATION (OPTIONAL)
# ================================
# Enable email notifications (true/false)
EMAIL_ENABLED=true

# SMTP server configuration (only needed if EMAIL_ENABLED=true)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=m.hamzashaikh6067@gmail.com
SMTP_PASSWORD=qsbrhwxsfymmwklo
SMTP_FROM=m.hamzashaikh6067@gmail.com
SMTP_FROM_NAME=Project Management System
SMTP_TLS=true

# ================================
# FILE UPLOAD SETTINGS
# ================================
# Maximum file upload size in bytes (10MB default)
MAX_UPLOAD_SIZE=10485760

# Allowed file extensions (comma-separated)
ALLOWED_EXTENSIONS=pdf,doc,docx,xls,xlsx,jpg,jpeg,png,gif,txt,csv

# Upload directory path
UPLOAD_DIR=./uploads

# ================================
# REDIS CONFIGURATION (For WebSocket - Optional)
# ================================
# Redis URL for WebSocket pub/sub
# Format: redis://host:port/db
# Leave empty if not using Redis
REDIS_URL=

# ================================
# LOGGING
# ================================
# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO
